#!/usr/bin/env node

const sqlite3 = require('better-sqlite3');
const { ChartCLI } = require('./../src/app/chart/cli');

const cli = new ChartCLI(
  sqlite3('./storage/olympic_history.db'),
);
cli.run(process.argv);
